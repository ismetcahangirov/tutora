/** Public query surface — the React Query client + provider (student epic #40). */
export { QueryProvider, type QueryProviderProps } from './query-provider';
export { createQueryClient, DEFAULT_STALE_TIME } from './query-client';
