/**
 * Profile feature — public barrel (student epic #40, #49).
 *
 * The student account + preferences screen (identity, appearance, language, saved
 * searches, sign-out). Import the screen into the route file:
 *   `import { ProfileScreen } from '@features/profile';`
 */
export { ProfileScreen, type ProfileScreenProps } from './screens/ProfileScreen';
