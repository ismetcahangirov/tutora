/**
 * Saved-searches feature — constants (student epic #40, #49).
 */

/** How many searches a student can keep. Beyond this, saving is rejected. */
export const SAVED_SEARCH_LIMIT = 10;

/** MMKV key for the persisted saved-search list. */
export const SAVED_SEARCH_STORAGE_KEY = 'saved-searches.list';
