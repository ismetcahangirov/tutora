import { API_PREFIX } from '@shared/lib';

/** Admin broadcast endpoint (relative to `VITE_API_URL`). */
export const ADMIN_NOTIFICATIONS_BROADCAST_ENDPOINT = `${API_PREFIX}/admin/notifications/broadcast`;
